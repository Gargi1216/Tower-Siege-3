/*class polygon  {
    constructor(x,y){
    this.polygon = Bodies.circle(50,200,20);
    this.x = 50;
    this.y = 200;
    this.r = 20;
    World.add(world,polygon)
    this.image = loadImage("hexagon.png");    
    }

    display(){
       var pos = this.body.position;
       push();
       translate(pos.x,pos.y);
       imageMode(CENTER);
       image(hexagon.png,polygon.position.x,polygon.position.y,40,40);
       pop();
    }
}*/